{
  "name": "exfer-honor-testnet-loop",
  "private": true,
  "type": "module",
  "description": "Continuous autonomous agent-to-agent settlement loop on the persistent exfer testnet: mine (seed node) -> quote -> settle -> honor, restartable, with a health/metrics endpoint.",
  "scripts": {
    "loop": "tsx loop.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "exfer-honor": "file:../.."
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "better-sqlite3": "^11.10.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
